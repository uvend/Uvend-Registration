# UVend Corporate Email Configuration
# Copy this to .env and update with your actual credentials

# Email Service (smtp, sendgrid, ses)
EMAIL_SERVICE=smtp

# SMTP Configuration for UVend
SMTP_HOST=smtp.office365.com
SMTP_PORT=587
SMTP_SECURE=false
EMAIL_USER=your-email@uvend.co.za
EMAIL_PASS=your-password
EMAIL_FROM=noreply@uvend.co.za

# Development Mode
NODE_ENV=development

# Alternative Configurations (uncomment if needed)

# If UVend uses Google Workspace:
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_SECURE=false

# If UVend has custom email server:
# SMTP_HOST=mail.uvend.co.za
# SMTP_PORT=587
# SMTP_SECURE=false

# If using SendGrid (ask IT for API key):
# EMAIL_SERVICE=sendgrid
# SENDGRID_API_KEY=your-sendgrid-api-key
# EMAIL_FROM=noreply@uvend.co.za

# If using AWS SES (ask IT for credentials):
# EMAIL_SERVICE=ses
# AWS_ACCESS_KEY_ID=your-access-key
# AWS_SECRET_ACCESS_KEY=your-secret-key
# AWS_REGION=us-east-1
# EMAIL_FROM=noreply@uvend.co.za
